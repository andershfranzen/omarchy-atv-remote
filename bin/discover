#!/bin/bash
set -euo pipefail
export PATH=/usr/bin:/bin
plugin_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
exec /usr/bin/python -E -s -B "$plugin_dir/bin/runner.py" 35 /usr/bin/python -E -s -B "$plugin_dir/bin/runtime.py" discover "$@"
